//! ken-mcp — stdio MCP server over ken-core. Stub until the mcp-server
//! change lands; compiling it keeps ken-core honest as a shared library.

fn main() {
    eprintln!("ken-mcp: the MCP server ships in a later Ken release; this build is a placeholder.");
    std::process::exit(1);
}
